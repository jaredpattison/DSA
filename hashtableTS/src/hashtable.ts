'use strict';

import LinkedList = require('../../singlyLinkedList/src/singly-linked-list');
// import util = require('util');
// import { isEmptyBindingElement } from 'typescript';
// import { cursorTo } from 'readline';

class Hashtable {
  public size: number;
  public map: 
  
  constructor(size) {
    this.size = size;
    this.map = new Array(size);
  }
}