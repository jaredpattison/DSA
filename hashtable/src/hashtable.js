'use strict';

const LinkedList = require('../../singlyLinkedListTS/src/singly-linked-list.js');
const util = require('util');

class Hashtable {
  constructor(size) {
    this.size = size;
    this.map = new Array(size);
  }

  hash(key) {
    return key.split
  }
}